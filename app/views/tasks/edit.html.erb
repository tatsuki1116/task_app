<div class="task_form">
	<h3>New Task</h3>
	<%= form_for @task do |f| %>
		<%= f.label :task_name %>
		<%= f.text_field :task_name %><br>
		<%= f.label :details %>
		<%= f.text_area :details %>
		<%= f.label :deadline_at %>
		<%= f.date_select :deadline_at %>
		<%= f.submit "編集" %>
	<% end %>
</div>